/**
 * Shared input/label class names for the demo CRUD dialogs so every form looks
 * consistent without each page redefining the same Tailwind strings.
 */

export const inputClass =
    "h-9 w-full rounded-md border border-input bg-white px-3 text-sm focus:outline-none focus:ring-1 focus:ring-ring";

export const labelClass = "mb-1 block text-xs font-medium text-slate-500";
